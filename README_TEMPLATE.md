# Membership Kit (Template)
Reusable membership/tier UI (HTML from v1.0) + config stubs (`template/config/*`).
- Copy `.env.sample` to `.env.local` and fill Stripe keys (if needed).
- Edit `tiers.yaml` and `features.yaml`.
- Serve static pages or wire into your app.
